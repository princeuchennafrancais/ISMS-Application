import 'package:flutter/material.dart';

class TransactNote extends StatelessWidget {
  const TransactNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
